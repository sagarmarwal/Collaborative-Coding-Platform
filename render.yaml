services:
  - type: web
    name: collaborative-coding-platform
    env: docker
    plan: free
    autoDeploy: true
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "5000"
      - key: PISTON_API_URL
        fromService:
          type: web
          name: piston-api
          property: host
    healthCheckPath: /
    
  - type: web
    name: piston-api
    env: image
    plan: free
    image:
      url: ghcr.io/engineer-man/piston:latest
    autoDeploy: false
    envVars:
      - key: PORT
        value: "2000"
      # This is the magic key that moves the 'isolate' and 'jobs' folders to a writable area
      - key: PISTON_DATA_DIRECTORY
        value: /tmp/piston
    healthCheckPath: /api/v1/health
    
  - type: redis
    name: coding-platform-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList:
      - source: 0.0.0.0/0
        description: Allow connections from app service